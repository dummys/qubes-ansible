---
- name: Download Nvidia repositories
  command:
    cmd: "curl -L -o /tmp/cuda-repo.deb {{ nvidia_cuda_url }}"
- name: Install Nvidia repositories
  command:
    cmd: dpkg -i /tmp/cuda-repo.deb
- name: Update APT
  ansible.builtin.apt:
    upgrade: "dist"
    update_cache: true
- name: Install Nvidia drivers
  ansible.builtin.apt:
    name:
      - nvidia-driver-cuda
      - nvidia-kernel-dkms
    state: present
